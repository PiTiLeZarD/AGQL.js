const styles = (theme) => ({
    card: {
        margin: "1em auto",
        position: "relative",
    },
    sectionTitle: {
        borderBottom: "1px dashed black",
    },
    fab: {
        position: "absolute",
        bottom: theme.spacing(2),
        right: theme.spacing(2),
        zIndex: 1000,
    },
});
export default styles;
